import { StrictStore, createKey } from 'strict-store';
import { describe, test, expect, beforeEach } from 'vitest';

describe.skip('Size method', () => {
  beforeEach(() => {
    StrictStore.clear();
  });
});
